# Optionally set the backend base URL for the React app.
# If not provided, the app will default to same host with port 3001.
# Example:
# REACT_APP_BACKEND_URL=https://vscode-internal-29809-beta.beta01.cloud.kavia.ai:3001
REACT_APP_BACKEND_URL=
